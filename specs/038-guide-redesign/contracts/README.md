# Contracts: Guide Redesign

No external interface contracts required. This feature is entirely frontend — it reads existing event state from `useEventContext()` and renders static guide content. No new API endpoints, no new data formats, no external integrations.
