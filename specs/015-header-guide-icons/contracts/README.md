# API Contracts: Header Guide Icons

**Feature**: `015-header-guide-icons`  
**Date**: 2026-02-25

## Overview

This feature is **frontend-only**. No new API contracts, endpoints, or modifications to existing endpoints are required.

The admin guide continues to read event state from the existing `EventContext` (populated via `GET /api/events/:eventId`). This is an existing read-only dependency, not a new contract.
